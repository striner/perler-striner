# 轻量终端 AI 图像像素化任务清单（草案）

## 阶段 1：规格确认

- [x] 确认最终拼豆颜色量化交给前端，后端不承担独立的非拼豆颜色量化。
- [ ] 验证并确认检测/分割模型工件、许可证和获取来源；允许替换不可用候选。
- [x] 确认 Prompt 支持多主体、允许为空，空值默认选择最显眼主体；失败回退浏览器。
- [x] 确认多主体逐实例分割后合并 Mask，并采用首版默认置信度、实例上限和 Mask IoU 去重参数。
- [x] 确认 Prompt 分隔格式、数量/长度限制和复杂自然语言能力边界。
- [x] 确认 POC 使用 NVIDIA CUDA + PyTorch，热启动单图目标 `3` 秒、并发 `1`、请求超时 `30` 秒。
- [x] 确认主体内部/轮廓保护区差异化采样、模式可用性行为和 `tiny_model@1.0.0` 标识。
- [ ] 在 CUDA 验收设备确定 PyTorch/CUDA 版本、显存峰值和模型缓存大小。

## 阶段 2：模型与运行时

- [ ] 验证并锁定检测器、分割器版本、哈希和许可证。
- [ ] 实现离线模型预取、完整性校验、缓存目录和加载状态。
- [ ] 增加 CUDA GPU 并发 `1`、排队和 `30` 秒超时配置，确保 PyTorch 推理不阻塞 ASGI 事件循环。

## 阶段 3：后端算法

- [ ] 创建 `algorithms/tiny_model/` 包及 descriptor、params、model_store、runtime、detector、segmenter、pipeline、service。
- [ ] 接入统一 `AlgorithmInput` / `AlgorithmOutput` 和标准错误信封。
- [ ] 实现 Prompt 检测、轻量分割、边缘增强、主体内部/轮廓保护区差异化采样和固定尺寸 RGBA 输出。
- [ ] 注册 `tiny_model` 生产算法并保留 `cv_native`。

## 阶段 4：前端与回退

- [ ] 增加始终可见的 AI 模式、能力驱动的可用状态/原因和 Prompt 输入。
- [ ] 增加 Generate/等待/Download 状态与生成期间控件锁定。
- [ ] 接入后端失败、空响应和非法输出的浏览器回退及既有通知。

## 阶段 5：验证与文档

- [ ] 编写后端和前端自动化测试，覆盖多主体 Mask 合并、空 Prompt 和浏览器回退。
- [ ] 使用真实样本与 `browser_native`、`cv_native` 对比主体/背景/边缘指标。
- [ ] 在 NVIDIA CUDA 设备验证启动预热、热启动单图 `3` 秒目标、并发排队、`30` 秒超时和显存峰值。
- [ ] 记录可复现命令、性能数据、未覆盖场景和剩余风险。
- [ ] 模型工件不提交 Git，检查日志和构建产物不含图片内容。
